DATABASE_URL="file:V:/sistema para vender autos/prisma/dev.db"
APP_TIMEZONE="America/Santiago"
AUTH_SECRET="cambia-este-secreto-por-uno-largo-de-32-caracteres-o-mas"
INITIAL_ADMIN_NAME="Victor Herrera"
INITIAL_ADMIN_EMAIL="victorherrera@sergioescobar.cl"
INITIAL_ADMIN_PASSWORD="cambiar-esta-clave-temporal"
RESEND_API_KEY=""
RESEND_FROM=""
BLOB_READ_WRITE_TOKEN=""
TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""
# Dominio real del sistema. Se usa para armar los enlaces de "recuperar
# clave"; si queda mal, el correo llega con un enlace que no abre.
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Llave con la que se cifra el token de Instagram antes de guardarlo en
# base. Minimo 16 caracteres. Si se cambia, el token guardado deja de
# poder leerse y hay que volver a conectar la cuenta.
PANEL360_SECRET_KEY=""
# Protege los endpoints /api/cron/*. En Vercel se configura tambien
# como variable del proyecto.
CRON_SECRET=""

# Produccion en Vercel + Supabase:
# DATABASE_URL="postgresql://postgres.xxxxx:password@aws-0-sa-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
# DIRECT_URL="postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres?sslmode=require"

# Secreto compartido con Panel360 para validar el token de entrada (SSO).
# Debe ser EXACTAMENTE el mismo valor en los dos proyectos.
PANEL360_SSO_SECRET=""
